# 🏁 地平线 6 · 自动助手 v1.2

一个 **Forza Horizon 6** 的挂机自动助手:自动买车加点 + EventLab 刷图循环、拍卖场抢车、清理重复车、一键买光未拥有的车、统计还没拥有的车及获取方式。**只通过虚拟 Xbox 手柄操作 + 只读截屏识别,不注入游戏、不修改任何游戏文件。**

> 直接下载下面的 `Forza6HelperV4GUI.exe`,双击运行即可,无需安装 Python。

## 🔄 v1.2 更新内容(相比 v1.1)
- **跑图剩余时间框**:刷图(跑图)期间,界面顶部会显示一个醒目的「⏱ 本轮跑图剩余 MM:SS」倒计时框;最后 30 秒会变红提醒,跑完自动隐藏。
- **新功能「统计未拥有的车」(只读不买)**:在『车辆收藏』网格页,自动逐辆扫描所有**还没拥有**的车(灰色「DISCOVER」格),读出每辆车的**获取方式**,最后给出一份按获取方式分组的统计报告(同时存到 `logs/未拥有统计.txt`)。**全程只按『购买』看弹窗再取消,绝不真的买。** 获取方式的识别是**用游戏内实采的上百条文案**训练出来的,覆盖:车展、抽奖、季节赛事/嘉年华奖励、收集簿各类别(危险标志/一日游/测速区间/地平线传奇赛/车库…)、车辆专精树、商店/DLC、谷仓车(寻车任务)。

## 🔄 v1.1 更新内容(相比 v1.0)
- **适配简体中文更新**:游戏把「车辆熟练度」改名为「车辆专精」,**买车加点流程已兼容**(已在游戏内实测通过)。
- **「买未拥有的车」长时间挂机更稳,不再卡死**:
  - 手柄掉线(弹「控制器未连接」)会**自动按 A 重连**并继续。
  - 卡住自愈:万一漂移到异常画面,会**自动退回、重新导航回『车展』网格页**继续买。
  - 加了超时看门狗,并把运行日志存到程序旁边的 `logs/gui.log`(方便出问题时排查)。

## ✨ 功能一览

| 功能 | 说明 |
|---|---|
| **模式三循环** | 买车(斯巴鲁 22B)+ 加满车辆专精 → 导航到 EventLab 刷分赛 → 视觉刷图 → 收尾,如此无限循环。可选「先买车再跑图」或「先跑图再买车」。跑图期间顶部显示本轮剩余时间。技术点数耗尽会自动关掉提示继续,不再卡死。 |
| **拍卖场抢车** | 在「搜寻」页设好车型 + 最高买断价,程序不停重搜,目标一出现就买断(**只买断、绝不竞价**)。 |
| **清理重复车** | 用游戏自带「重复项」筛选,批量删掉多余的某型号车(自动**保留收藏的 / 正在驾驶的**那辆,删前核对确认框)。 |
| **买未拥有的车** | 在车展自动勾上「未拥有」筛选,一辆辆把没有的车全买下来,直到买完。 |
| **统计未拥有的车(只读)** | 在『车辆收藏』网格页,扫描所有没拥有的车,识别每辆车的获取方式(车展/抽奖/季节奖励/收集簿各类别/车辆专精树/商店/谷仓车)并生成分组报告。**只看不买。** |
| **把地平线调成 16:9** | 一键把游戏窗口规范成 16:9,让识别在任意显示器(含带鱼屏)上都稳定。 |

> 每个功能都有独立的「停止」键,随时可停;数量不设上限,跑到完成或你手动停为止。

## 🖥️ 现代界面
- **横向深色现代界面**,运行日志/报告在**右侧整条**显示。
- 大字体、扁平按钮、深色标题栏与窗口融为一体,整体更清爽。

## 📦 怎么用
1. 下载 **`Forza6HelperV4GUI.exe`**,双击运行(首次打开约十几秒,在加载识别模型)。
2. 需要 **ViGEmBus** 虚拟手柄驱动;没装的话程序会提示并自动打开下载页,装完重开即可。
3. **游戏建议用「窗口 / 无边框窗口」模式**,运行时保持 Forza 在前台(失焦会自动切回)。
4. 各功能的起始页面:
   - **模式三**:停在**暂停菜单**,点「开始」。
   - **拍卖抢车**:进 拍卖场 → 搜索拍卖,设好过滤,停在**「搜寻」配置页**。
   - **清理重复**:停在**我的车辆 / 车辆标签页**。
   - **买未拥有**:进 车辆 → 购买车与二手车 → 车展,停在**车辆网格页**。
   - **统计未拥有**:进 收集簿 → 旅行家 → 车辆收藏,停在**车辆网格页**。

## 🛡️ 原理与安全
- **只发标准虚拟 Xbox 手柄输入**(通过 ViGEmBus),和你自己拿手柄按没有区别。
- **只读截屏 + 本地识别**(YOLO / OCR / 规则),**不读写游戏内存、不注入、不修改任何游戏文件**。
- 需要游戏保持前台(失焦时自动切回,属于正常的窗口切换)。

## ⚠️ 免责声明
本工具仅供学习与研究自动化技术。使用任何游戏自动化都可能违反游戏服务条款,由此产生的一切后果(包括但不限于账号风险)由使用者自行承担。

---
🤖 本版本由 Claude Code 协助开发。
